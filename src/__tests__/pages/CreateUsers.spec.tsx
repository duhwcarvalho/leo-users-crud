import React from 'react';
import { render } from '@testing-library/react';
import CreateUsers from '../../pages/CreateUsers';

describe('ListUsers Page', () => {
  it('should be possible to list users', () => {
    // const { debug } = render(<CreateUsers />);

    // debug();
  });
});